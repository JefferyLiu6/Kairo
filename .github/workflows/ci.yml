name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: web/pnpm-lock.yaml

      - name: Install web dependencies
        run: pnpm --dir web install --frozen-lockfile

      - name: Lint
        run: make lint

      - name: Test
        run: make test

      - name: Judge benchmark contract check (no model calls)
        run: make eval-judge

      - name: Upload judge contract report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: judge-replay-report
          path: artifacts/judge-replay.json

      - name: Web build
        run: pnpm --dir web build
